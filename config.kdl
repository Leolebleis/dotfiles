// Two-Mode Workspace -- Zellij Config
// Synced across Windows, macOS, Linux via code meta-repo

// Appearance
theme "catppuccin-mocha"
pane_frames false
simplified_ui true
default_layout "clean"
default_mode "normal"
mouse_mode true
scroll_buffer_size 50000
show_startup_tips false
show_release_notes false

// Default shell (Windows uses pwsh; macOS/Linux fall back to $SHELL)
default_shell "pwsh"

ui {
    pane_frames {
        hide_session_name true
    }
}

// Plugins
plugins {
    zj-quit location="https://github.com/cristiand391/zj-quit/releases/download/0.3.1/zj-quit.wasm"
    zellij-forgot location="https://github.com/karimould/zellij-forgot/releases/download/0.4.2/zellij_forgot.wasm"
    autolock location="https://github.com/fresh2dev/zellij-autolock/releases/download/0.2.2/zellij-autolock.wasm" {
        is_enabled true
        triggers "nvim|vim|git|fzf|zoxide|atuin|lazygit|helix"
        reaction_seconds "0.3"
    }
}

// Plugins to load on session start
load_plugins {
    autolock
}

// Keybindings -- identical across all platforms
// Alt-based bindings avoid Ctrl/Cmd platform differences
keybinds {
    shared_except "locked" {
        // Split panes
        bind "Alt \\" { NewPane "Right"; }
        bind "Alt -" { NewPane "Down"; }

        // Close pane
        bind "Alt w" { CloseFocus; }

        // Navigate panes (vim-style + arrow keys)
        bind "Alt h" "Alt Left" { MoveFocus "Left"; }
        bind "Alt l" "Alt Right" { MoveFocus "Right"; }
        bind "Alt j" "Alt Down" { MoveFocus "Down"; }
        bind "Alt k" "Alt Up" { MoveFocus "Up"; }

        // Tabs
        bind "Alt t" { NewTab; }
        bind "Alt r" { SwitchToMode "RenameTab"; TabNameInput 0; }
        bind "Alt 1" { GoToTab 1; }
        bind "Alt 2" { GoToTab 2; }
        bind "Alt 3" { GoToTab 3; }
        bind "Alt 4" { GoToTab 4; }
        bind "Alt 5" { GoToTab 5; }
        bind "Alt 6" { GoToTab 6; }
        bind "Alt 7" { GoToTab 7; }
        bind "Alt 8" { GoToTab 8; }
        bind "Alt 9" { GoToTab 9; }

        // Floating panes
        bind "Alt f" { ToggleFloatingPanes; }

        // Fullscreen current pane
        bind "Alt z" { ToggleFocusFullscreen; }

        // Resize mode
        bind "Alt =" { Resize "Increase"; }
        bind "Alt +" { Resize "Increase"; }
        bind "Alt _" { Resize "Decrease"; }

        // Cheatsheet (zellij-forgot)
        bind "Alt e" {
            LaunchOrFocusPlugin "zellij-forgot" {
                floating true
                move_to_focused_tab true
            }
        }

        // Confirm-before-quit (zj-quit)
        bind "Ctrl q" {
            LaunchOrFocusPlugin "zj-quit" {
                floating true
                move_to_focused_tab true
            }
        }
    }

    renametab {
        bind "Enter" { SwitchToMode "Normal"; }
        bind "Esc" { UndoRenameTab; SwitchToMode "Normal"; }
    }
}
